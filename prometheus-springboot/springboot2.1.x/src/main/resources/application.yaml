server:
  port: 8080
spring:
  application:
    name: springboot2.1.x
management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'
  metrics:
    tags:
      application: ${spring.application.name}